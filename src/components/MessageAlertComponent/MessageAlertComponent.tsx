import React from "react";
import MessageAlertCards from "@/container/MessageAlertCards";

const MessageAlertComponent = () => {
  return (
    <div>
      <MessageAlertCards />
    </div>
  );
};

export default MessageAlertComponent;
